'use strict';

export enum ERightAngle {
    an0 = 0,
    an90 = 90,
    an180 = 180,
    an270 = 270,
}

export class RotateTransducers {
    static do(d: ImageData, angle: ERightAngle): void {
        d;
        angle;
    }
}
