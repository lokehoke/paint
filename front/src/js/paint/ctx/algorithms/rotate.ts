'use strict';

export enum IRightAngle {
    an0   = 0,
    an90  = 90,
    an180 = 180,
    an270 = 270,
};

export class RotateTransducers {
    static do(d: ImageData, angle: IRightAngle): void {

    }
};